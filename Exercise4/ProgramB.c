#include <stdlib.h>

int main() {
    // Linux / WSL
    system("gnome-terminal -- ./programA");

    // Windows version (uncomment if on Windows)
    // system("start programA.exe");

    return 0;
}
